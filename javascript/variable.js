/* 자바스크립트에서 변수를 선언할때는,
    변하지 않는 값은 const,
    변할 수 있는 값은 let으로 선언한다

    변수 규칙
    1. 변수는 문자와 숫자, $와 _만 사용한다
    2. 첫글자는 숫자가 될 수 없다
    3. 예약어는 사용할 수 없다
    4. 가급적 상수는 대문자로 알려준다
    5. 변수명은 읽기 쉽고 이해할 수 있게 선언한다
*/

let name1 = "Mike";
let age = 30;
const PI = 3.14;



alert(name1); 
console.log(age);   