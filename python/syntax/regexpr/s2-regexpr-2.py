# 정규 표현식(Regular Expressions)

# 정규 표현식에 사용하는 모듈

# 표현식을 컴파일하여 컴파일된 패턴 객체를 리턴
# import re
# pattern = re.compile('ab*')

# 정규식을 사용한 문자열 검색
# match() : 문자열의 처음부터 정규식과 매치되는 조사
# search() : 문자열 전체를 검색하여 정규식과 매치되는 조사
# finall() : 정규식과 매치되는 모든 문자열(substring)을 리스트로 리턴
# finditer() : 정규식과 매치되는 모든 문자열(substring)을 반복가능한 객체로 돌려준다.